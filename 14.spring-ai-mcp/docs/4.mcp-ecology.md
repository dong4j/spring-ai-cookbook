# MCP 生态里的其他角色

<!--
https://developers.openai.com/codex/cli/reference#codex-mcp-server
https://github.com/tuannvm/codex-mcp-server
https://nacos.io/en/blog/nacos-gvr7dx_awbbpb_ahggmtqmxwndm22k/

写作思路：
- 这一篇是「生态篇」，多举几个 MCP 周边产品，扩展读者视野：
  - Nacos MCP 网关（REST → MCP 转换）
  - codex mcp-server（命令行工具 + MCP Server）
  - IDE / Agent 等其他 MCP client 实例（简单提一下）
- 重点仍然围绕“如何把传统能力（REST / CLI）变成 MCP 工具”。

大纲示例：
1.	MCP 不只是规范，也不只是 Spring AI 的一块组件
  - 简单列一下目前常见 MCP 产品 / 项目
  - 本文只挑两个典型：Nacos MCP 网关 & codex mcp-server
2.	Nacos MCP 网关：把 REST API 变成 MCP 工具
  - 背景：企业里的大量能力都是 REST API
  - 核心思路：
    - 通过 MCP Server 封装这些 REST
    - 对 LLM / Agent 来说，它们就是统一的 MCP 工具
  - 简述网关工作机制：
    - 接收 MCP 的 tools.call
    - 映射到对应 REST 路由
    - 执行 HTTP 请求，返回结果包装回 MCP 格式
3.	codex mcp-server：让命令行工具暴露成 MCP 能力
  - 回顾你之前用 Python 调 codex mcp-server 的例子
  - codex 作为 MCP Server 在做什么：
    - 暴露「本地代码操作 / 项目理解」能力为 MCP tools
  - 演示：任意 MCP Client（包括 Spring AI MCP Client）都能接入 codex
  - 说明：codex 让「AI + 项目代码」之间的交互标准化
4.	其他 MCP 产品快速浏览（点名即可）
  - MCP-aware IDE / 插件
  - 各种语言实现的 MCP Server SDK
  - MCP Inspector 再提一下：生态里的调试利器
5.	小结：MCP 正在把「工具世界」标准化
  - 工具的提供者可以是：REST 服务、CLI 工具、微服务、云接口…
  - 工具的使用者可以是：Claude、ChatGPT、Spring AI 应用、IDE Agent…
  - 这样往后延伸：标准的另一头总会暴露出问题与局限。

过渡到第 6 篇：

任何协议一旦在生态中被广泛使用，就一定会暴露出新的问题。
下一篇我们就来聊聊 MCP 自身的局限：
- 安全模型太弱
- 工具暴露过多能力
- 工具 / 资源描述占用大量上下文
这些问题，又会如何推动下一步的演进？
-->
